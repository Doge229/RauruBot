# logger.py

import os
import datetime
import pytz
import inspect
import discord
from discord.ext import commands, tasks
from discord import app_commands
import messages
import system
import config
import json
import random
import asyncio

class Logger():
    def __init__(self) -> None:
        pass

# create log file if not
    
# log function to be called, combine print with it
    # error
    # system
    # admin
    # dev?

# way to send a log file though discord